# AGENTS.md

## Scope and Precedence

These instructions apply to the entire repository unless a more specific `AGENTS.md` exists in a subdirectory. More specific instructions override broader ones for files under their scope.

Committed topic guidance lives in `.agent/instructions/`. Ephemeral notes, scratch work, checklists, and handoffs belong under `.agent/sessions/`.

When instructions conflict, follow this order:

1. System, platform, and user instructions.
2. The nearest applicable `AGENTS.md`.
3. Task-relevant `.agent/instructions/*.md`.
4. `.agent/docs/tech-software-spec.md` and other project documentation.
5. Existing code, tests, and configuration.

`.agent/docs/tech-software-spec.md` is the current product and architecture source of truth. Its normative requirements take precedence over summaries in agent documents. If documentation, code, tests, or instructions disagree, identify the conflict and resolve the affected sources together; ask the user when intended behavior is unclear.

## Repository Facts

- The project is the CollectivIQ OpenAI-Compatible Gateway: a local or privately hosted HTTP service between OpenCode and CollectivIQ.
- The compatibility target is the bounded OpenAI Chat Completions profile needed by `@ai-sdk/openai-compatible`, not the full OpenAI API.
- The reference stack is current supported Node.js LTS, strict TypeScript, Fastify, native `fetch` or Undici, schema validation, Pino, Prometheus-compatible metrics, Vitest, npm, and Docker.
- Each completion is stateless from the public API's perspective and creates exactly one new CollectivIQ thread in the initial architecture.
- Only the typed CollectivIQ adapter may know upstream endpoint paths and wire schemas.
- The gateway translates tool definitions and model-generated requests but never executes OpenCode tools.
- Input is text-only in the initial release. Responses support non-streamed output and buffered synthetic SSE streaming.
- Prompt content, source code, file paths, tool arguments/results, and model answers are neither logged nor retained by default.
- Emulated tool calling is **supported opt-in beta**: all numerical release gates in `.agent/docs/tech-software-spec.md` section 30 pass (completed 2026-09-01 campaign), and on that evidence it graduated from experimental to supported opt-in beta. It stays **non-default and OpenCode permission-gated**; beta is not production readiness, repeatability, or a cross-account/cross-version guarantee, and default enablement is a separate decision after the Phase 4 controls. Section 30 owns the graduation decision — summarize it elsewhere, never restate its evidence.
- The project has a runnable foundation: configuration loading/validation (including bounded gateway-key parsing — ≤64 keys, ≤8192 UTF-8 bytes/key), a Fastify server construction factory, `/healthz` and `/readyz` routes, credential-redacting logging, graceful shutdown, tests, Docker packaging, and CI. It also has the **Phase 1A** authenticated public model surface (offline, no live CollectivIQ call): mandatory gateway client authentication on every `/v1/*` route (`Authorization: Bearer` vs `COLLECTIVIQ_GATEWAY_KEYS`, case-insensitive scheme, exact-match token, fixed-length SHA-256 + `timingSafeEqual`, fixed `401`), an immutable virtual-model catalog/resolver (exact-case, config-order, one captured `created` timestamp per server instance), public OpenAI model objects, `GET /v1/models` and `GET /v1/models/:model`, and shared bounded OpenAI error envelopes (`401`/`404`/`500`). Health/readiness stay unauthenticated, and none of these routes — `/healthz`, `/readyz`, and the Phase 1A model metadata endpoints — call CollectivIQ. It also has an offline (Phase 0) CollectivIQ adapter boundary under `src/collectiviq/`: the production adapter for the three core operations, a bounded/cancellation-aware transport, a normalized content-free error model with method-aware retry metadata, provisional response validation, a shared dual-mode upstream credential provider (`src/collectiviq/auth.ts`: `bearer` default plus an OAuth2 password-exchange mode via `POST /login`, login **verified-live**, selected by `COLLECTIVIQ_AUTH_MODE`), an opt-in staged discovery session/CLI (preflight by default; token-inspection and abort discovery disabled), value-free cleanup diagnostics and a content-free recovery journal plus a recovery-only `contract:discovery:cleanup` command, a committed filtered OpenAPI snapshot of **ten** allowlisted operations (`contract/collectiviq/`), bounded extraction tooling (`scripts/openapi/`), and hermetic contract tests (`test/contract/`). The production adapter is now **wired into the authenticated, non-streamed Phase 1B `POST /v1/chat/completions` path** (detailed in the Phase 1B entry below); its discovery/recovery tooling remains operator-only and outside the public request path. A real completion request can call CollectivIQ, but import, construction, tests, and the build smoke do not; a user-observed, sanitized live OpenCode/CollectivIQ **foreground transport** smoke was reported on **2026-08-15** (protocol-mode `collectiviq-claude` response returned, synthetic streaming completed, tool metadata accepted and discarded with no tool call), on which the returned response objected to the gateway's serialized protocol wrapper as embedded identity/instruction manipulation. The `collectiviq-claude-direct` prompt-serialization profile (`promptMode: "direct"`, latest-user-only, no protocol wrapper) is the mitigation and the committed OpenCode default, and a sanitized, user-authorized **2026-08-18** smoke **observed it resolve that refusal for the tested account** (a natural coding request returned a relevant answer, synthetic streaming completed with no protocol objection / tool alert / tool call, and the hidden `collectiviq-fast` title request returned a valid title on its first attempt), so the Phase 1 semantic exit criterion is **met for the tested account**; this is a sanitized single-account observation, **not** production readiness or a repeatable upstream guarantee — a combined answer, a long-running streaming duration, and general non-Claude routing remain unverified and any further live run is approval-gated. Four authorized authenticated baseline discovery runs were executed: two on 2026-08-06 and 2026-08-07 in `bearer` mode (both failed strict completeness), and two on **2026-08-11 in `password` mode that BOTH passed strict completeness (exited zero)** with **identical** sanitized results (`evidenceFormatVersion` 2). Password `POST /login` and the core create/submit/messages contract are therefore **verified-repeatable** and encoded into synthetic fixtures (`processAccepted202`, `messagesCreateTime`; no live value committed); `validation.ts` now maps `createdAt` from `create_time`. Thread-deletion outcomes were **credential/principal-dependent** (cause not established): the password/member principal deleted its own newly created thread (`200`, repeated) while re-deleting that same just-deleted id returned `403`; the API-key principal's own-thread delete returned `403` (2026-08-07); a cross-principal recovery attempt also returned `403` — consistent with a permission/scope check, but the provider's evaluation order is unconfirmed, so recovery's exact-`404` convergence was not exercised. Phase 0 has advanced substantially but is not auto-declared complete (open items: idempotency, ordering/pagination, prompt/rate limits, retention, native tools, true-streaming/SSE scope, token lifetime). Text-only synthetic SSE streaming (`stream: true`) is now implemented offline (**Phase 2**, detailed below). **Supported opt-in beta emulated tool calling is implemented offline (Phase 3, detailed below); it is non-default and OpenCode permission-gated. Five authorized live evaluator campaigns have been executed. The latest and operative one — the state-aware report-v5 / checkpoint-v4 campaign completed live on 2026-09-01 — scored the full corpus with **all eight section-30 gates passing** and overall **`passed: true`**, so the numerical section-30 criteria are **met** and Phase 3 graduated to **supported opt-in beta**, staying non-default and OpenCode permission-gated. The 2026-08-31 report-v4 campaign (six of eight gates passing) is historical evidence. **Specification section 30 owns every campaign detail, gate number, and diagnostic; `.agent/instructions/validation.md` owns the operational validation detail. Do not restate them here.** Baseline evaluator hardening landed before the 2026-08-26 campaign; the report-v4 / checkpoint-v3 corrections (genuine one-user agent loop, deterministic content-safe synthetic tool results, truthful early termination, `executedScenarioRounds` ledger, v1/v2 checkpoints rejected) were added afterwards and were first exercised live on 2026-08-31; the state-aware report-v5 / checkpoint-v4 corrections came later still and were first exercised live on 2026-09-01, so neither set describes the earlier campaigns.** Redis, metrics/tracing, and idempotency features remain unimplemented, as do **true** upstream streaming and **native** tool mode; those paths are still planned until they actually exist. (Gateway client authentication and the public model endpoints are implemented per Phase 1A above.)
- **Phase 1B is implemented (offline; live upstream only at request time):** an authenticated, non-streamed, text-only `POST /v1/chat/completions` wired through the typed CollectivIQ adapter. It normalizes the OpenAI request (`src/openai/chat-request.ts`, `messages.ts`, `chat-types.ts`), rejects deferred features with stable `400`s, resolves the internal model policy (`ModelCatalog.resolveModel`), serializes the prompt through the model-policy-aware selector (`src/prompts/serializer.ts`) driven by the resolved model's normalized `promptMode` — `protocol` uses the deterministic versioned full-history serializer (`src/prompts/conversation.ts`), `direct` uses the latest-user-only serializer (`src/prompts/direct.ts`) — enforces process-local global + per-key capacity with a bounded queue (`src/generation/capacity.ts`), acquires capacity **before** creating exactly one upstream thread, submits once (never retrying `create_thread`/`process_message`), polls `get_messages` with a total deadline, capped 1.25 backoff + jitter, GET-only retry, and desired-source selection (`src/generation/polling.ts`), orchestrates the flow (`src/generation/chat-completion.ts`), and encodes the non-streamed response with zero/unavailable usage (`src/openai/chat-response.ts`). Client-disconnect + total-deadline + shutdown share one abort path; a deadline maps to `504`, a client disconnect sends no body, a shutdown cancellation (client still connected) maps to `503`. The runtime credential provider is built from validated config without re-reading the environment (`buildCredentialProviderFromConfig`), and construction opens no socket and makes no CollectivIQ/login call. Each completion creates a **new** CollectivIQ thread; the gateway retains no prompt/answer content after the request, but provider-side retention/training/deletion remain unknown. The orchestration is now split into a synchronous `prepare` (resolve model + serialize prompt + enforce `maximumPromptBytes` + mint the stream-stable id/`created`) and an async `run` (capacity → one thread → one submit → authoritative poll → trusted text) so the API layer owns JSON-vs-SSE encoding; all Phase 1B guarantees (capacity before thread creation, release on every exit path, no `create_thread`/`process_message` retry, authoritative deadline/cancellation, trap-safe error identity) are preserved. The gateway performs exactly one `create_thread` per completion request, sending the fixed content-free placeholder `THREAD_TITLE` (exactly `New Thread`); a sanitized 2026-08-18 observation found `New Thread` to be the minimal gateway-compatible trigger that causes CollectivIQ to natively generate a server-side, prompt-related thread title after `process_message` (URL-encoded request otherwise unchanged, `is_title_from_user=false`, `llms_explicitly_set=true`). That provider title is derived and persisted asynchronously and entirely provider-side; the gateway never derives, logs, caches, or retains it, and it is distinct from any OpenCode session title. It is read only **transiently** — via a new OBSERVED-ONLY/provisional `getThreadTitle` adapter op that issues a bounded, no-retry `GET /get_threads` and reads only the single target thread's `title` (pending until a validated ready title; account/principal-dependent) — to serve the new authenticated `GET /v1/opencode/session-title` **extension route** (`src/api/opencode-title-route.ts`; a CollectivIQ/OpenCode extension, NOT part of the OpenAI profile; `200 ready`/`202 pending`+`Retry-After: 2`/`400`/`404`, always `Cache-Control: no-store`, never leaking the thread id/title/body). The trusted `CompletionResult` now carries an internal `upstreamThreadId` used ONLY for process-local native-title correlation, never exposed in JSON/SSE output. A process-local correlation service (`src/opencode/title-bridge.ts`), keyed by `gatewayKeyId + sessionId` via the optional opaque `X-CollectivIQ-OpenCode-Session-ID` header (1–128 bytes `[A-Za-z0-9_-]`, never logged/reflected), stores ONLY the two opaque ids + the upstream thread id (never a title/prompt/answer), first-registration-wins, TTL 60 s (lazy), global cap 128 / per-key cap 32, single-flight, ≥2 s between upstream lookups, ≤6 lookups per correlation; it registers only after a confirmed success (non-streamed after `run()`; streamed only after terminal chunk AND `data: [DONE]`), is synchronous/bounded/non-throwing, and a restart safely loses pending propagation. OpenCode's hidden LLM `title` agent is now **disabled** in the committed `opencode.jsonc` (`"title": { "disable": true }`), so it creates NO separate title thread/completion; a first foreground message creates exactly one upstream thread, and a plugin (`.opencode/plugins/collectiviq-native-title.ts`, committed project-local by default and optionally installed globally via a supported manual symlink for cross-project use, process-wide idempotent across both loads via a `Symbol.for(...)` `globalThis` singleton) propagates the native title via that extension endpoint (best-effort; never overwrites a manual/already-propagated title; failure leaves the default). The plugin **default-exports the OpenCode V1 `{ id: "collectiviq-native-title", server }` module** so OpenCode 1.18.21's loader (`readV1Plugin`) invokes only `default.server` and never enters the legacy export scan. The **confirmed 2026-08-21 root cause** was that the plugin's earlier bare-**function** default fell through to that legacy scan, which rejects the first non-function runtime export (e.g. `UPDATE_TIMEOUT_MS`) with `Plugin export is not a function`, so the plugin **never loaded** (global-symlink and project-local paths alike) and no header/correlation/poll/rename behavior ran — the 2026-08-21 smoke did prove one foreground thread, no hidden title thread, and provider-native title generation, but the OpenCode rename failed for this reason. Separately, the `chat.headers` provider match is descriptor-safe and tolerates BOTH the nested SDK shape (`provider.info.id`) and the flat OpenCode-runtime shape (`provider.id`); this is implemented and hermetically tested as **offline hardening** and is **not** the proven live root cause (provider matching never ran on 2026-08-21). After the loader fix, a trace showed the poller reach base-URL resolution but stop before its first title lookup because the plugin resolved its gateway key ONLY from `process.env.COLLECTIVIQ_GATEWAY_KEY` (absent). The plugin now performs ONE bounded, descriptor-safe connection resolution that reuses the resolved CollectivIQ provider credential (`provider.collectiviq.options.apiKey`, merged-over-embedded) with `COLLECTIVIQ_GATEWAY_KEY` read only as a **lazy injected fallback confined to the production wrapper** (key accepted only as a non-empty ≤8192-UTF-8-byte string with no unresolved `{env:…}`/`{file:…}` placeholder, used exactly, kept local to the poll and never logged/cached/stored); the provider-config→environment precedence and lazy fallback are hermetically verified. A sanitized, user-authorized **2026-08-22 live smoke observed the COMPLETE propagation path succeed for the tested local configuration** (OpenCode 1.18.21): exactly one new foreground thread, no hidden title thread, a provider-native title generated for it, and the OpenCode top-level session title changed from its default to that provider-native title (the foreground response completed and was relevant, no alert/tool call). This is a sanitized single-local-configuration observation — **not** production readiness, a cross-account/cross-version guarantee, or a claim about which credential source was exercised; propagation stays best-effort/bounded/non-fatal, a failure leaves the OpenCode default/manual title, and further live runs remain approval-gated. `collectiviq-fast` remains declared for manual/other-account use but is no longer the title agent's model, so the earlier "two or more upstream threads per session" no longer applies to the committed config. The protocol-mode foreground `collectiviq-claude` path objected to the serialized protocol wrapper on **2026-08-15**, but the committed-default `collectiviq-claude-direct` (`promptMode: "direct"`) path was **observed to resolve that refusal for the tested account** on **2026-08-18** (valid answer + historical valid first-attempt `collectiviq-fast` title observed before the hidden title agent was disabled; sanitized single-account observation, not production readiness); a combined answer, a long-running streaming duration, and general non-Claude routing remain unverified and any further live run is approval-gated.
- **Phase 2 is implemented (offline; live upstream only at request time):** text-only buffered **synthetic SSE** for `POST /v1/chat/completions` with `stream: true`, served by the same route alongside the non-streamed JSON path. Request normalization (`src/openai/chat-request.ts`, `chat-types.ts`) carries a normalized `stream: boolean` (absent/`false` → JSON, exactly `true` → SSE, every other value → stable `400`). The pure frame encoder + deterministic code-point-safe split live in `src/openai/chat-stream.ts` (target 128 / max 256 / min 32 code points; paragraph→sentence→whitespace boundaries; concatenation reproduces the answer exactly); the SSE transport — header commit after preparation succeeds, an assistant-role opener before any upstream work, a `: collectiviq-gateway keep-alive` comment every 15 s while polling, backpressure-aware serialized writes, keep-alive timer cleanup, a terminal `finish_reason: "stop"` chunk then `data: [DONE]`, content-free `data: {"error": …}` records (a `503` on shutdown only while the transport is still writable — an undrainable/failed-terminal response is force-closed, on a bounded `res.end()` fallback if needed, and may end silently), and write-failure/disconnect cancellation that stops polling and releases capacity — lives in `src/api/chat-stream-response.ts`, driven from `src/api/chat-completions-route.ts`. No `usage` is emitted on a stream; tool-call streaming is now implemented for `toolMode: "emulated"` models (Phase 3, opt-in beta — see the Phase 3 entry below); streaming is synthetic (polling-backed), not true upstream streaming. A basic live synthetic-streaming request completed end-to-end from OpenCode on **2026-08-15**, but the long-running / keep-alive streaming smoke test is **not run**.
- **Phase 2.1 is implemented (offline): an OpenCode text-compatibility bridge, NOT Phase 3 tool calling.** OpenCode attaches `tools`/`tool_choice` to every request even when all tool permissions are denied, so `validateChatRequest` (`src/openai/chat-request.ts`) now takes the catalog's `resolveModel`, returns the resolved `VirtualModel` with the frozen request, and runs a **model-policy-aware** bridge AFTER model resolution (validate `tools` first, then `tool_choice`). A tool definition is never semantically interpreted, retained, serialized into the upstream prompt, forwarded, logged, reflected, persisted, or executed; it is traversed ONLY through data-property descriptors (`getOwnPropertyDescriptor`/`Reflect.ownKeys`, no `[[Get]]`) for bounded JSON-shape and byte accounting, so submitted accessors and executable hooks (getters, `toJSON`, iterators) are never invoked. For a `toolMode: "disabled"` model it TOLERATES the tool metadata: it accepts an own `tools` array of ≤`MAX_TOOLS` (128) entries whose entire JSON encoding is ≤`MAX_TOOL_SCHEMA_BYTES` (2 MiB, spec §21.6) and a `tool_choice` of exactly `"auto"`/`"none"`, records ONLY the parameter name in `X-CollectivIQ-Ignored-Parameters`. The array length is read from its own DATA descriptor, and byte accounting is a bounded iterative descriptor traversal (cycle- and depth-guarded). For a disabled model, actual tool CALLING stays off: a `required`/named `tool_choice`; a non-array, over-128, or over-2-MiB `tools`; an accessor, cycle, sparse/exotic/over-deep structure, unsupported value, or descriptor/proxy failure anywhere; or ANY `tools`/`tool_choice` presence against a `native` model (not implemented) is rejected with a stable content-free `unsupported_parameter` `400` (`param` = `tools`/`tool_choice`) — an `emulated` model instead normalizes and RETAINS the policy and can emit model-proposed calls (Phase 3, supported opt-in beta / non-default; see the Phase 3 entry below); `response_format`/`logprobs`/`audio`/image-binary remain presence-rejected, and message `tool_calls`/tool-role are presence-rejected for disabled/text-only models but PARSED for emulated models. The route (`src/api/chat-completions-route.ts`) now resolves the model via the validator (no separate `resolveModel`/`404` block). The committed `opencode.jsonc` sets the primary agent `model`, the top-level `model`, and `small_model` all to `collectiviq/collectiviq-claude-direct` (wildcard permission still `deny`), and **disables** OpenCode's hidden built-in `title` agent in the committed `opencode.jsonc` (`"title": { "disable": true }`), so it creates NO separate title thread/completion (a first foreground message creates exactly one upstream thread; native-title propagation is instead handled by the `collectiviq-native-title` plugin (project-local by default; optionally installed globally; process-wide idempotent) via `GET /v1/opencode/session-title` — see the Phase 1B entry above, including the historical 2026-08-21 sequence (the plugin entry module never loaded because its bare-function default was rejected by OpenCode's legacy export scan, then the poller resolved its key only from an absent env var; both fixed — the V1 `{ id, server }` default and provider-config credential reuse — with the complete path observed to succeed for the tested local configuration in a sanitized 2026-08-22 live smoke, a single-local-configuration observation and not a production/cross-account/cross-version guarantee); the earlier "two or more upstream threads per session" no longer applies). `collectiviq-fast` stays declared for manual/other-account use but is no longer the title model; the sanitized 2026-08-18 observation that a `collectiviq-fast` title request succeeded on its first attempt is HISTORICAL (predating the disable) and does not prove general non-Claude routing. The Claude-only foreground/`small_model` default is used because non-Claude sources are currently blocked account-side (generic gateway prompts were classified as Atlassian queries; `selected_llms`/`generate_combined`/`llms_explicitly_set=true` gave no verified override; the filtered OpenAPI snapshot exposes no generic-routing field) — this is a value-free, account-specific observation that does not generalize. The protocol-mode `collectiviq-claude` and the consensus/coder/fast virtual models remain declared. Hermetic unit/integration/contract coverage plus the pinned-SDK compatibility suite (a real function tool + `toolChoice: "auto"` through streamed and non-streamed paths → ordinary text, `finish_reason: "stop"`, no tool call) are added; these hermetic checks make no live upstream/OpenCode/network call and no tool execution occurs. Separately, the live smoke on **2026-08-15** confirmed the bridge in situ: OpenCode's auto-attached tool metadata was accepted and discarded, no "tools is not supported yet" alert occurred, and no tool call was emitted.
- **Phase 3 (emulated tool calling) is implemented (offline; SUPPORTED OPT-IN BETA, NON-default, OpenCode permission-gated; live upstream only at request time).** A new `src/tools/` boundary implements the emulated engine: `copy.ts` (`safeJsonCopy` — a descriptor-safe bounded deep copy to trusted plain JSON that never triggers a getter/`[[Get]]`/`toJSON`/iterator and fails closed on accessors/cycles/sparse/exotic/over-deep/non-finite/symbol/function/bigint and the byte/depth bounds), `schema.ts` (`compileToolset` — a per-request Ajv compile that selects the meta-schema dialect from each tool schema's ROOT `$schema`: **draft-07** default when `$schema` is absent/boolean-root, and **draft-07** and **draft 2020-12** each recognized by an exact URI allowlist (`http`/`https`, with/without trailing `#`) so OpenCode 1.18.21's draft-2020-12 built-in tool schemas compile, while a non-string or any other `$schema` fails closed; at most one lazily-created Ajv instance per dialect per call; no coercion/defaults/property-removal, no remote `$ref`, no cross-request retention; `ajv-formats` for both dialects), `protocol.ts` (`parseToolEnvelope` — the strict §12.2 algorithm: Unicode trim → remove at most one complete outer JSON fence → real JSON parse → validate `gateway_protocol:"1.0"` + `type` + exact key set → allowlist names → validate args against the compiled schema → enforce strict/choice/count/parallel/arg-byte limits; no regex-from-prose, no partial-fence, no silent repair, no trust in upstream ids), `select.ts` (`selectGeneration` — §12.3: valid desired answer-source → individual-source consensus grouped by canonical call set, scored by summed `percent_usage` when every valid individual has one else agreement count, ties broken by configured `selectedLlms` order; deterministic; `auto` falls back to the desired source's raw text; `required`/named with no valid call → `502 invalid_tool_response`), `ids.ts` (`call_ciq_<ULID>`), `request.ts` (`normalizeToolRequest` + prior tool-history linkage/uniqueness/schema validation — orphan/duplicate/unresolved/mismatched relationships rejected before upstream work), `normalize.ts`, `canonicalize.ts`, and `limits.ts` (the single source of truth for `MAX_TOOLS`=128, `MAX_TOOL_SCHEMA_BYTES`=2 MiB, `MAX_TOOL_ARGUMENT_BYTES`=1 MiB, `MAX_TOOL_CALLS_PER_RESPONSE`=8, `MAX_TOOL_JSON_DEPTH`=512; `chat-request.ts` re-exports `MAX_TOOLS`/`MAX_TOOL_SCHEMA_BYTES`). Pinned direct deps added: `ajv` 8.20.0, `ajv-formats` 3.0.1, `ulid` 2.4.0 (scoped lockfile change; eslint keeps its nested `ajv@6`). Config enforces `toolMode: "emulated"` REQUIRES `promptMode: "protocol"`; `toolMode: "native"` stays rejected at request time. For a `toolMode: "emulated"` model the request boundary (`chat-request.ts` resolves the model BEFORE per-message normalization; `messages.ts` is model-aware; `NormalizedRole` adds `"tool"`; `NormalizedMessage` gains nullable `content` + `toolCalls`/`toolCallId`; `NormalizedChatRequest` gains `tools`/`toolChoice`/`parallelToolCalls`) NORMALIZES and RETAINS the tool policy, compiles the toolset once, validates prior tool history, and carries the compiled toolset on `ChatRequestResult` (not the frozen request); `conversation.ts` adds the versioned `tool-or-final` control prompt + `AVAILABLE_TOOLS_JSON` block when tools are active, and the text-only prompt is byte-for-byte unchanged; `polling.ts` returns the validated message snapshot (`selectWinningMessage`); `chat-completion.ts` returns a discriminated `CompletionResult` (`text` | `tool_calls`, both carrying `upstreamThreadId`) and maps a required/named choice with no valid call to `502` (`INVALID_TOOL_RESPONSE_ERROR`); `chat-response.ts`/`chat-stream.ts` encode `tool_calls` (JSON: `content:null`, `finish_reason:"tool_calls"`, zero/unavailable usage; SSE: one complete indexed delta with the trusted ids + a `tool_calls` terminal + `[DONE]`, no `usage`); native-title correlation still registers after a confirmed success for both result variants. The disabled-mode Phase 2.1 bridge (tolerate-and-discard) is unchanged. **In emulated mode the validated tool schemas, prior arguments, and tool results ARE serialized into the prompt sent to CollectivIQ (they are never logged or retained); each tool-loop round creates a new upstream thread; the gateway returns model-PROPOSED calls and NEVER executes, authorizes, or simulates a tool — OpenCode owns permissions and execution.** The opt-in `collectiviq-claude-tools` model (`config/models.example.yaml`; the ignored local `config/models.yaml` must be updated manually) is the single tool-enabled VIRTUAL MODEL, and `opencode.jsonc` exposes that one model through TWO functional, behaviorally identical tool-enabled AGENT ENTRIES (same model, `mode: "primary"`, wildcard permission `"ask"`): the canonical `collectiviq-tools-beta` and the DEPRECATED `collectiviq-tools-experimental` compatibility alias, which is a real second tool-enabled entry rather than a disabled stub, is retained through Phase 4, and can only be removed by a separately announced breaking configuration change. Every existing default (`collectiviq-text`, top-level `model`/`small_model` = `collectiviq-claude-direct`, and the declared `fast`/`coder`/`consensus`/`claude`/`claude-direct` models) selects neither agent and stays `toolMode: "disabled"`. Hermetic unit/integration/contract coverage plus the pinned-SDK compatibility suite (real `generateText`/`streamText` tool call + an in-memory three-step read/edit/test loop with synthetic tools) and a deterministic **adversarial corpus** (`npm run test:adversarial`, ≥200 protocol cases) pass; none makes a live upstream/OpenCode/network call and no tool execution occurs. The approval-gated **live evaluator** `npm run eval:tools` (`src/eval/`; preflight-by-default, password-only, fixed origin, 200 single-round + 20 three-step scenarios, hard cap 280 upstream completions, per-request immediate cleanup, ID-only recovery journal, abort-on-cleanup-failure, value-free output) has been run in five authorized campaigns; **specification section 30 owns every campaign detail, gate number, cleanup/checkpoint result, and diagnostic — do not restate them here.** The operative evidence is the state-aware report-v5 / checkpoint-v4 campaign completed live on **2026-09-01**: it scored the full corpus across two resumable execution segments, **all eight gates passed**, and overall **`passed: true`**, so the numerical section-30 criteria are **met** and Phase 3 graduated to **supported opt-in beta**, staying non-default and OpenCode permission-gated; no production prompt, parser, selector, evaluator, threshold, model default, or model configuration changed to reach that result, in response to it, or as part of graduation, and any further live run is a separate approval-gated decision. The 2026-08-31 report-v4 campaign (six of eight gates passing, `passed: false`) is historical evidence. Baseline evaluator hardening landed offline before the 2026-08-26 campaign (the report-v4 / checkpoint-v3 corrections came later and were first exercised live on 2026-08-31, and the state-aware report-v5 / checkpoint-v4 corrections later still and were first exercised live on 2026-09-01, so neither set describes the earlier campaigns): it now emits a versioned value-free output union (`preflight | progress | blocked | executed`) and a four-state gate status (`passed | failed | incomplete | not_evaluated` — a zero denominator is `not_evaluated`, never 0%; a partial sample is `incomplete`, never `passed`; a threshold gate is `passed`/`failed` only when its `plannedDenominator` is complete, carrying explicit `numerator`/`denominator`/`plannedDenominator`), overall `passed` requiring a complete corpus, no abort, all gates passed, zero cleanup/journal failures, AND successful checkpoint finalization; structured value-free abort diagnostics (stable reason + closed stage set + normalized `UpstreamError` code + safe HTTP status read trap-safely via `isUpstreamError` + a `resumable` flag — create-stage interruption is ambiguous and non-resumable, submit/poll failure resumable only after confirmed thread deletion + durable checkpoint persistence, cleanup/journal/checkpoint failures non-resumable); JSON progress events only after a cleaned attempt AND a durable checkpoint write; and a private content-free resume checkpoint at the ignored fixed path `.agent/sessions/eval/tools-eval-checkpoint.json` (0700 dir / 0600 file, `O_NOFOLLOW`, atomic temp+rename, strict version/origin/auth/corpus-fingerprint/bounded-count validation) gated behind a new `--resume-approved` flag (an existing checkpoint requires it; incompatible/absent-approval fails closed BEFORE any credential read or network I/O; `create`/`process_message` are never auto-replayed, only GET polling keeps its idempotent retry; a single-round cursor advances only after cleanup-confirmed, a multi-step scenario commits and advances only at scenario end so a mid-scenario interruption restarts that scenario; a first controlled SIGINT/SIGTERM cleans the recorded thread on an independent signal, a second forceful interrupt terminates, and the ID-only recovery journal stays the final recovery mechanism; on complete success the checkpoint is finalized/removed), plus a value-free password-auth observation (login attempts, last HTTP status or null, normalized boolean) in the final report. Four further remediations are now **enforced by code and hermetic tests** (`src/eval/checkpoint.ts`, `src/eval/report.ts`, `test/contract/eval-checkpoint.test.ts`): (1) **semantic (corpus-bound) checkpoint validation** — the executed evaluator builds `buildEvalCases()` EXACTLY ONCE per run and derives its fingerprint, plan, projection, and case-loop iteration from that ONE `EvalCase[]` value (`corpusFingerprint(cases)`, `evalPlan(cases)`, and `buildEvalCorpusProjection(cases)` all accept the supplied array, so no divergent rebuilt corpus can slip into the executed path); `buildEvalCorpusProjection` also FAILS CLOSED at build if any round's `choice.kind` is outside the closed diagnostic union `"auto" | "required" | "function"` (specifically `"none"`, which the synthetic corpus never uses and the diagnostic shape cannot represent), so no downstream constructor ever needs a silent `"none" → "auto"` relabel and a corpus containing `"none"` refuses to enter the pipeline before any credential read or network I/O. On resume a decoded checkpoint is validated against that fingerprint-bound `EvalCorpusProjection` (`buildEvalCorpusProjection(buildEvalCases())`, an immutable content-free per-case per-round structural view: `phase` + per-round `choiceKind`/`hasExpectedTool`) BEFORE any credential read or network I/O, with all planned bounds and per-round layout taken from the ACTUAL corpus (never checkpoint-claimed sizes, never an aggregate `first N rounds are expected` inference), and `validateResumableCheckpoint` re-checks every projected round's `choiceKind` against the diagnostic union as defense in depth: `nextCaseIndex` strictly within `0..corpus-length` (a resumable checkpoint can never encode a complete corpus — a genuinely complete run removes its checkpoint), committed single/multi case counts EXACTLY cursor-derived, gate denominators equal to the committed counts (single = committedSingle, multi = committedMulti, expected-call = committedSingle + committedMulti·3), every numerator an integer in `[0, denominator]`, and upstream-round counters bounded by the corpus-derived plan — a committed three-step scenario performs BETWEEN 1 and its corresponding case's `rounds.length` (4 for the uniform production corpus) upstream rounds under the revised early-termination semantics (read/edit/test/final answer only when the scenario ran to completion), NOT the three expected-tool-call rounds that form the gate denominator, so the committed upstream-round floor was `committedSingle + Σ executedScenarioRounds` under checkpoint v3 (a compact per-committed-multi-step-scenario ledger; each entry mapped in projection order to its corresponding committed multi-step case and bounded within `[1, that case's rounds.length]`; checkpoint v4 replaces it with the `scenarioEvidence` tuple described at the end of this bullet — the per-CASE round count, NOT the projection-wide `maxRoundsPerCase`, so a non-uniform corpus is validated round-by-round rather than reduced to the projection maximum; `.length` equals `completedMultiStepScenarios`) — NOT the old `committedSingle + committedMulti·maxRoundsPerCase`; the counters accumulate across resume segments, but a segment aborts at its first non-committing case, so they are also bounded ABOVE by that floor plus per-segment slack (`runSegments·(maxRoundsPerCase−1)` completed and `runSegments·maxRoundsPerCase` attempted — up to one in-flight scenario's uncommitted partial rounds plus one terminal failed round per segment), with completedRounds ≤ attemptedRounds, so an arbitrarily inflated counter is rejected; resumable cleanup accounting truthful (deleted + failed == attempted, failed == 0, journalFailures == 0, attempted == deleted == attemptedRounds), runSegments ≥ 1, and the fresh zero-count anchor valid — a forged/inconsistent checkpoint (including any "complete + passing, zero-attempt" claim) is rejected as content-free invalid state, so it can NEVER produce a zero-network `executed` pass; (2) **durable resumable-vs-blocked state** — the checkpoint schema carries `resumeState: "resumable" | "blocked"` plus a value-free closed abort `{ stage, reason }` (null when resumable), and every NON-resumable abort (ambiguous create, cleanup-delete failure, recovery-journal persistence failure, checkpoint-persist failure, journal-finalize failure, toolset-compile, signal/lifecycle) attempts to replace the checkpoint with a durable `blocked` **tombstone** (only closed lifecycle metadata + abort stage/reason — never prompts/answers/ids/credentials/titles/bodies), a `--resume-approved` run REJECTS a blocked checkpoint before credentials/network (recovery requires deliberate operator archival/removal — no automatic destructive restart), a failed tombstone write leaves the report non-resumable and truthfully surfaces the checkpoint persistence failure, and a complete successful run still removes the checkpoint; (3) **exact 0600 file mode + safe ancestry** — a checkpoint file is accepted only when `(mode & 0o777) === 0o600` (0400/0200/0000, group/world bits, non-regular, symlink all rejected), the checkpoint location is an explicit TRUSTED BASE plus its ordered managed components (`.agent`/`sessions`/`eval`), and EVERY managed component is `lstat`-validated top-down from the base as a real, non-symlink directory on read/write/delete/exists — so a symlink at ANY managed level whose descendants already exist through it is caught before the OS would traverse it (closing the earlier immediate-parent-only gap); directory creation no longer uses recursive `mkdir` (missing components are created ONE AT A TIME with the private 0700 mode and re-validated), the file open keeps `O_NOFOLLOW` with atomic temp+rename and bounded size, and nothing AT or ABOVE the trusted base is symlink-validated so a legitimate platform symlink above it is not falsely rejected; and (4) **lifecycle/output guarantees via one explicit finalization state machine** — the recovery journal is finalized EXACTLY ONCE on every executed AND blocked path after successful init through one idempotent helper (closing a prior gap where the working-tree evaluator did not finalize it on all paths — including the initial-anchor-write-failure path, which now routes the finalize through that helper instead of swallowing it, and reports a journal-finalize failure there as its OWN closed blocked reason (`recovery-journal-finalize-failed`), distinct from the `checkpoint-write-failed` anchor failure, before any credential read or network call), a journal-finalization failure is a closed abort stage/reason (`recovery-journal-finalize` / `recovery-journal-finalize-failed`), is non-resumable, durably blocks the checkpoint, and prevents a pass; progress ordering is exact (a cleaned-but-uncommitted TERMINAL resumable attempt — a submit/poll failure or interruption whose thread was created and confirmed deleted with no journal failure — now carries a bounded value-free pending-progress descriptor that finalization emits EXACTLY ONCE, and only AFTER the resumable checkpoint durably persists, so no cleaned attempt is silently dropped; the descriptor never claims the case cursor advanced; a journal-finalize or checkpoint persistence failure emits no resumability progress; no duplicate progress record for the same completed multi-step case); and all abort/blocked reasons are CLOSED literal unions (`AbortReason` / `BlockedReason` / `AbortStage`) constructed through typed helpers, so no free-form exception text ever reaches output or checkpoints. The overall finalization order is finish/abort round → bounded cleanup → finalize recovery journal exactly once → persist a validated resumable checkpoint OR a blocked tombstone → emit progress only when a resumable checkpoint durably persisted → emit final report → on complete success remove the checkpoint and report success only after all finalization succeeds. The numerical section-30 release gates are **met** by the completed 2026-09-01 report-v5 campaign (see the status above and specification section 30). Emulated tool mode is supported opt-in beta and stays non-default and OpenCode permission-gated. The current account may still reject the protocol wrapper; the default was not changed and parsing was not weakened to work around that. The draft-2020-12 dialect support closes the confirmed OpenCode 1.18.21 schema-compilation gap both **offline** (hermetic suites, including a pinned-SDK `read` tool declaring draft 2020-12) and in one **sanitized, user-authorized live smoke on 2026-08-24** (the then-experimental `collectiviq-tools-experimental` agent — now the deprecated alias of `collectiviq-tools-beta` — + `collectiviq-claude-tools` model): OpenCode's built-in `read` schema (draft-2020-12 root `$schema`) passed request validation with no `tools is not supported for this request.` warning; OpenCode presented a permission prompt, the user granted one-time approval, and `read` executed only after it; no other tool was requested/executed; and the post-tool completion returned a relevant final answer. Exactly two new CollectivIQ threads were observed — the tool-proposal completion and the post-tool-result final-answer completion — the expected stateless one-new-thread-per-completion-round tool-loop rounds, **not** a hidden title request (no hidden title-generation thread was created). This confirms the live OpenCode permission/execution boundary behaved correctly for the tested local/account configuration (the gateway only PROPOSED the call; OpenCode owned authorization/execution). It is a **single sanitized observation** — not production readiness, repeatability, a cross-account/cross-version guarantee, or proof of general provider routing — and the smoke threads are not claimed deleted (no cleanup result supplied). (This tool-schema smoke is a DISTINCT 2026-08-24 event from the `eval:tools` evaluator run described earlier in this bullet; do not conflate them.) The numerical section-30 release gates are **met** by the completed 2026-09-01 report-v5 campaign (specification section 30 owns the campaign evidence; post-2026-08-26 review found three evaluator ambiguities — fixed synthetic tool result, a fresh user message per step, and a non-terminating loop — which the report-v4 correction below resolves, so only the 2026-08-31 and later campaigns' numbers reflect the corrected evaluator). To make a diagnostic rerun scientifically valid the evaluator has been revised offline to model a **genuine OpenCode-style agent loop** (spec §30): each multi-step scenario carries an `EvalScenarioState` (`path`, `initialContent = "version=1"`, `expectedFinalContent = "version=2"`) plus a mutable per-run transition state (owned by `src/eval/scenario-engine.ts` under v5); ONE initial user message (`rounds[0].prompt`) states the whole goal and later rounds accumulate history ONLY through the previously accepted assistant `tool_calls` message and exactly linked `role: "tool"` synthetic result messages (the evaluator NEVER injects a fresh user instruction between tool results). Synthetic tool results were rendered by a dedicated renderer (v5 folds this into the shared transition engine) and are deterministic and content-safe (no filesystem, shell, MCP, external service, repository content, or real user data): `read` → `{"ok":true,"path":<state.path>,"content":<state.content>}` (supplies the content the model needs to construct the expected `edit`); `edit` → `ok:true` only when the call's `path` matches the scenario path AND `text` matches the expected replacement (state flips to `content = expectedFinalContent`, `edited = true`), else `ok:false`; `test` → `{"ok":true,"testsPass":<content === expectedFinalContent>}`. A scenario STOPS at its FIRST terminal failure (premature final text, no valid call, unavailable, unauthorized call, allowed-but-unexpected tool, invalid transcript linkage) and issues no further upstream rounds; remaining planned expected-tool rounds count as gate MISSES in the section-30 denominators — never as attempted upstream rounds — and NO cascade diagnostics are fabricated for rounds that never ran. Section-30 thresholds and planned denominators are **unchanged**: a terminated scenario still contributes `expectedCallsPerScenario` (3) to the expected-call denominator and 1 to `multi.total`, and a scenario counts as multi-success only when every expected round produced the correct allowed call AND the scenario ran to completion; `plannedUpstreamRounds` (280) is the complete-corpus UPPER BOUND, not the exact attempt count — a complete failed corpus reports strictly fewer `attemptedRounds` when multi-step scenarios terminate early. That evaluator emitted **report version 4** with the bounded value-free `diagnostics.failures` collection (only on `executed` reports; then a closed nine-member `EvalFailureReason` union, since extended to ten by v5 below; deterministic classification; at most one primary diagnostic per failed multi-step scenario at its terminal failure round; identity `(caseOrdinal, roundOrdinal)`; never contains prompts, answers, arguments, schemas, tool/model names, ids, credentials, titles, bodies, urls, timestamps, or exception text) and persisted it via **checkpoint format version 3**, since superseded by v4 below (the compact `[caseOrdinal, roundOrdinal, reasonCode]` diagnostic ledger described above PLUS a per-committed-multi-step-scenario `executedScenarioRounds` ledger — one integer per committed multi scenario, in commit order, each mapped in projection order to its corresponding committed multi-step case and bounded within `[1, that case's rounds.length]` — the per-CASE round count, NOT the projection-wide `maxRoundsPerCase`, so a non-uniform corpus is validated round-by-round rather than reduced to the projection maximum — `.length` equals `completedMultiStepScenarios`; the committed upstream-round floor is `committedSingle + Σ executedScenarioRounds` — with strict corpus-bound semantic validation against the freshly built `EvalCorpusProjection` before any credential read or network I/O; rehydration derives `phase`/`choiceKind` from the actual case/round and fails closed on any impossible entry — unknown code, out-of-corpus case/round, or an out-of-diagnostic-union `choiceKind` such as `"none"` — never silently skips; `MAX_CHECKPOINT_BYTES` stays at 8192; **v1 and v2 checkpoints are rejected with no migration path**; multi-step diagnostics commit only on whole-scenario commit; a resumed final report re-emits every prior segment's diagnostics exactly once). That corrected **report v4 / checkpoint v3** evaluator was first exercised live in the completed 2026-08-31 campaign summarized above, which is now historical; the operative campaign is the 2026-09-01 report-v5 campaign, so the numerical section-30 criteria are met and emulated tool mode is supported opt-in beta while staying non-default and permission-gated. **A separate approval-gated, multi-step-only `eval:tools:diagnose` command is implemented (spec §30.1) to explain those value-free `expected-tool-not-invoked` round-2 failures; ONE live run has completed under its historical v2 classifier and its current v3 path has NOT been run — see the state-aware entry at the end of this bullet.** It runs only the 20 multi-step scenarios at global corpus ordinals 201–220 (max 80 upstream rounds, fixed origin, password-only, same approval flags plus `--resume-approved`), reuses the shared `src/eval/live-round.ts` lifecycle extracted from `tools-eval-cli.ts` (both evaluators now consume ONE implementation; adding the diagnostic changed none of the release evaluator's behavior, report/checkpoint versions, flags, thresholds, corpus, gate accounting, checkpoint path, or exit semantics) and the release evaluator's own failure classifier, and adds three CLOSED value-free dimensions (`allowedCallRelation`, `selectionSource`, `callMultiplicity`) beside the existing ordinals/`choiceKind`/`reason`. In its HISTORICAL **v2** form `allowedCallRelation` was HISTORY-aware: because the round request enables parallel tool calls, one accepted round can invoke several tools, so the relation was judged against the names the scenario ACTUALLY invoked in accepted earlier rounds plus the tools later planned rounds still expect, and a round whose expected tool already ran reported the appended member `expected-already-invoked` instead of a fabricated `future-only`. (v3 below judges the same buckets against SUCCESSFULLY COMPLETED transitions and removes that member.) It emits an INDEPENDENT output union — **v2** then, **v3** now — with `profile: "multi-step-transition"`, **no gate collection and no `passed` field** — only `completed`, which exits ZERO even when model transition failures were observed — and persists resume state in a SEPARATE diagnostic checkpoint (`.agent/sessions/eval/tools-multi-step-diagnostic-checkpoint.json`; format **v2** then, **v3** now, with formats 1 and 2 rejected and no migration) owned by a self-contained module that imports nothing from `checkpoint.ts`, so it structurally cannot read, overwrite, finalize, or remove the release checkpoint. A resumable diagnostic checkpoint NEVER encodes a complete-corpus cursor: the final scenario's commit stays in memory (no progress record either) and finalization disposes of it, so the last durable file always remains resume-valid and an interruption before finalization replays exactly that final scenario. Diagnostic versions move INDEPENDENTLY of the release evaluator's report/checkpoint versions. **Specification section 30.1 owns the normative contract and `.agent/instructions/validation.md` owns the operational detail; do not restate them here.** No production prompt, parser, selector, threshold, model default, model configuration, OpenCode configuration, or public route changed. **ONE live diagnostic run has since completed** under its historical v2 classifier (20/20 scenarios, 54 rounds attempted and completed, 54/54 threads deleted, zero cleanup/journal failures, a finalized checkpoint, no abort; 7 scenarios followed the old static schedule and 13 failed at round 2 with `expected-already-invoked`). It established that the static round-2 `edit` expectation was STALE — an accepted parallel batch had already invoked `edit` — but NOT that those edits SUCCEEDED, because the v2 relation tracked names rather than transitions. **Both evaluators are therefore now STATE-AWARE; the release evaluator's state-aware form was exercised live as the completed 2026-09-01 campaign, while the diagnostic's v3 path has NOT been run live.** A shared evaluator-only module `src/eval/scenario-engine.ts`, consumed by BOTH live CLIs so they cannot drift, models the ordered synthetic workflow `read → edit → test → final text` with prerequisite-gated transitions (`read` needs the exact synthetic path; `edit` needs a successful `read` plus an exact path+text match; `test` needs a successful `edit` and matching content), folds a parallel batch in the model's RETURNED order (so `[read, edit]` advances two transitions and `[edit, read]` advances only `read`), keeps failed/repeated/out-of-order calls in the transcript without advancing state, and derives each round's expectation from the next UNSATISFIED transition rather than the tool named at that ordinal. All state is synthetic and process-local: no filesystem, shell, MCP, external service, repository content, or real user data, and no tool execution. The release evaluator now measures gate evidence PER PLANNED TRANSITION (independent schema/argument/expected-name/satisfied flags per step, merged across retries so no step is double-counted, extras completed in a parallel batch receiving full evidence, each step contributing exactly one unit to the expected-step denominator at scenario commit); a scenario succeeds only when all three transitions AND an accepted final text complete within four rounds — which parallelism can reach in three; an expected tool that is present but fails SEMANTICALLY keeps its step pending for a later bounded retry instead of terminating; and a budget that ends without an accepted final answer emits exactly one appended `scenario-round-budget-exhausted` (reason code `10`, scope `"any"`). `EVAL_REPORT_VERSION` is `5` and `CHECKPOINT_FORMAT_VERSION` is `4` (**formats 1, 2, and 3 rejected, no migration**), with `executedScenarioRounds` replaced by the per-committed-scenario tuple `[executedRounds, satisfiedSteps, schemaMask, nameMask, argMask]` (counts and bitmasks only, never a tool name). A multi-step diagnostic's scope is judged from the persisted SATISFIED STATE, never the round's corpus slot (a single-round case still uses its own `hasExpectedTool`); the release evaluator — like the diagnostic — keeps the FINAL case's commit in memory and never persists a complete resumable cursor, through ONE shared commit path used by both the single-round and multi-step branches; and a value-free corpus/engine preflight guard requires every multi-step case's declared expected-tool sequence to equal the engine's ordered `read → edit → test` EXACTLY (a correct count in the wrong order is rejected) before any credential read or network I/O. Validation additionally rejects a ledger-length mismatch, an out-of-range executed-round or satisfied count, a mask bit outside the planned step count, a mask omitting a satisfied prefix bit, an aggregate numerator disagreeing with the mask popcounts, a terminal diagnostic whose scope disagrees with the satisfied state, a diagnostic beyond the executed rounds, a diagnostic-free scenario with incomplete state, and a failed scenario claimed as successful — all before any credential read or network I/O, with `MAX_CHECKPOINT_BYTES` unchanged at 8192. The diagnostic moves to report v3 / checkpoint v3 (**formats 1 and 2 rejected**), judges prior/future relations against SUCCESSFULLY completed transitions, REMOVES the v2 member `expected-already-invoked` as unreachable (preserved only as historical v2 evidence), persists a per-scenario `[executedRounds, satisfiedSteps]` tuple, and stays non-gating with complete checkpoint isolation. **Corpus sizes, thresholds, the 280-round upper bound, the 260 expected-step denominator, and all single-round behavior are UNCHANGED; report v5 has now been run live as the completed 2026-09-01 campaign, which is the last scored campaign, so the numerical section-30 criteria are met and Phase 3 is supported opt-in beta while staying non-default and permission-gated. The diagnostic's own v3 path remains NOT run live and establishes no release gate.**
- **The `promptMode` virtual-model property and the `direct` prompt profile are implemented (offline).** Each virtual model carries a `promptMode` selecting prompt serialization: `protocol` (default) is the existing full-history `COLLECTIVIQ GATEWAY PROTOCOL` header + versioned JSON conversation envelope (`src/prompts/conversation.ts`, byte-for-byte unchanged); `direct` submits ONLY the last normalized `user`-role message content, verbatim, with no protocol header, JSON envelope, `BEGIN_/END_CONVERSATION_JSON` markers, role labels, prefixes, suffixes, or other messages (`src/prompts/direct.ts`). The field is OPTIONAL in the model YAML (schema `Type.Optional`) and normalized to `protocol` when omitted, so existing/ignored model files are unchanged; the loaded internal `VirtualModel` always has an explicit `promptMode`, and it is never exposed in public `/v1/models` objects. Serializer selection is a model-policy-aware router (`src/prompts/serializer.ts`, the `PromptSerializer.serialize(request, promptMode)` port) driven from the validated `promptMode` — never a model-id string — dispatched in `chat-completion.ts` `prepare` before the UTF-8 `maximumPromptBytes` check (so in direct mode only the selected latest-user content is measured) and failing closed on an impossible mode. A `promptMode: "direct"` request with **no** user-role message is rejected at the model-aware `validateChatRequest` boundary with a fixed content-free `400 invalid_request_error` (`param: "messages"`, `code: "invalid_request"`) BEFORE prepare/capacity/thread/submit/SSE. The `direct` profile is intentionally lossy and stateless (it drops system/developer instructions, assistant history, and earlier user turns), is NOT a role-preserving translation, and is NOT prompt-injection prevention (spec §8.4.1, §34.2). The example catalog adds `collectiviq-claude-direct` (`promptMode: direct`, five models total), which is the committed OpenCode foreground/`small_model` default; the ignored local `config/models.yaml` must be updated manually before that default resolves. A sanitized 2026-08-18 smoke **observed** this profile resolve the 2026-08-15 semantic refusal **for the tested account**, meeting Phase 1's valid-answer exit criterion for that account — a single-account observation, not production readiness or a repeatable upstream guarantee.
- Implemented source lives under `src/` (`index.ts`, `server.ts`, `api/health-route.ts`, `api/gateway-auth.ts`, `api/models-route.ts`, `api/v1-routes.ts`, `api/chat-completions-route.ts`, `api/chat-stream-response.ts`, `api/opencode-title-route.ts`, `opencode/title-bridge.ts`, `openai/errors.ts`, `openai/models.ts`, `openai/chat-types.ts`, `openai/messages.ts`, `openai/chat-request.ts`, `openai/chat-response.ts`, `openai/chat-stream.ts`, `prompts/conversation.ts`, `prompts/direct.ts`, `prompts/serializer.ts`, `generation/model-catalog.ts`, `generation/types.ts`, `generation/capacity.ts`, `generation/polling.ts`, `generation/chat-completion.ts`, `generation/seams.ts`, `generation/runtime.ts`, `config/`, `observability/logger.ts`, `shared/redaction.ts`, `collectiviq/`, the emulated tool engine `tools/` (`limits.ts`, `types.ts`, `copy.ts`, `ids.ts`, `schema.ts`, `normalize.ts`, `protocol.ts`, `canonicalize.ts`, `select.ts`, `request.ts`, `index.ts`), and the approval-gated live evaluators `eval/` (`tools-eval-cli.ts`, `cases.ts`, `report.ts`, `checkpoint.ts`, the SHARED `live-round.ts` round lifecycle, the SHARED `scenario-engine.ts` state-aware transition engine, and the multi-step transition diagnostic's `tools-diagnostic-cli.ts`, `diagnostic-report.ts`, `diagnostic-checkpoint.ts`)); the committed upstream contract snapshot under `contract/collectiviq/`; extraction tooling under `scripts/openapi/`; tests under `test/` (including `test/contract/`, the separate hermetic `test/compatibility/` suite run only by `npm run test:compatibility`, and the separate hermetic `test/adversarial/` release-gate corpus run only by `npm run test:adversarial`). The grounded upstream contract is documented in `.agent/docs/collectiviq-upstream-contract.md`. Package scripts, `package.json`, and `package-lock.json` exist — inspect them rather than assuming (`ajv`/`ajv-formats`/`ulid` are pinned direct **runtime** dependencies for the emulated tool engine; the pinned `ai` / `@ai-sdk/openai-compatible` **dev** dependencies back the compatibility suite only).

## Read First

Before editing:

1. Read this file.
2. Inspect the working tree (`git status --short` when this is a Git worktree) and preserve user changes.
3. Read the task-relevant instruction file from `.agent/instructions/`.
4. Read the relevant sections of `.agent/docs/tech-software-spec.md` and any owner document identified by `.agent/instructions/documentation.md`.
5. Inspect directly related code, tests, and configuration if they exist.

Do not read every instruction file or the entire specification by default. Follow task routing and load only the relevant material.

## Task Routing

| Task touches | Read |
| --- | --- |
| Repository initialization, dependency selection, package scripts, TypeScript config, or initial scaffolding | `.agent/instructions/project-initialization.md`, `.agent/instructions/validation.md`, `.agent/instructions/security.md` |
| Package/module boundaries, service orchestration, state machine, IDs, timeouts, or shared utilities | `.agent/instructions/project-map.md`, `.agent/instructions/validation.md` |
| OpenAI request/response schemas, `/v1/models`, chat completions, errors, message normalization, or SSE | `.agent/instructions/openai-compatibility.md`, `.agent/instructions/validation.md` |
| CollectivIQ HTTP calls, upstream schemas, polling, response selection, retries, timeouts, or fixtures | `.agent/instructions/upstream-integration.md`, `.agent/instructions/validation.md`, `.agent/instructions/security.md` |
| Prompt serialization, tool schemas, tool parsing, candidate selection, or tool-call streaming | `.agent/instructions/tool-calling.md`, `.agent/instructions/openai-compatibility.md`, `.agent/instructions/security.md` |
| Auth, secrets, logging, privacy, limits, retention, Redis, dependencies, or untrusted data | `.agent/instructions/security.md` |
| Configuration, health/readiness, metrics, tracing, concurrency, cancellation, Docker, or shutdown | `.agent/instructions/operations.md`, `.agent/instructions/security.md`, `.agent/instructions/validation.md` |
| Tests, mocks, compatibility checks, adversarial cases, load tests, or release gates | `.agent/instructions/validation.md` plus the relevant domain instruction |
| Documentation-only or documentation-impacting changes | `.agent/instructions/documentation.md` |

## Core Workflow

1. Inspect applicable instructions, relevant specification sections, and directly related files before planning edits.
2. Preserve existing user changes and ignore unrelated files.
3. Use `rg` and `rg --files` for discovery.
4. For non-trivial work, make a short plan covering outcome, affected boundaries, validation, documentation impact, and approval gates.
5. Implement the narrowest complete change that respects the architecture and current delivery phase.
6. Validate with the smallest relevant test or check first, then broaden in proportion to risk.
7. Update the owning documentation when behavior, contracts, architecture, operations, security, or terminology changes.
8. Review the final diff for unrelated changes, speculative scaffolding, generated artifacts, weakened tests, sensitive content, and contradictions.

Use `.agent/sessions/<branch-or-task>/<worker-id>/` only for ephemeral collaboration artifacts. Durable decisions must be moved into source documentation before the task is complete.

## Command Policy

`package.json` defines the canonical scripts; prefer them and the locked package manager (npm, `packageManager` pinned) over ad hoc tool commands. Inspect the file before citing or running a script. The default aggregate check is `npm run validate` (format check, lint, typecheck, tests, build, compiled-import smoke test).

Implemented canonical scripts: `dev`, `build`, `start`, `typecheck`, `lint`, `format`, `format:check`, `test`, `test:unit`, `test:integration`, `test:contract`, `test:coverage`, `test:build`, `validate`, plus the standalone hermetic `test:compatibility` (own `vitest.compatibility.config.ts`), the standalone hermetic `test:adversarial` (own `vitest.adversarial.config.ts`; the ≥200-case tool-protocol release-gate corpus), and the network-only, opt-in `contract:openapi:check`, `contract:openapi:refresh`, `contract:discovery`, `contract:discovery:cleanup` (the recovery-only cleanup command), `eval:tools` (the approval-gated live tool evaluator), and `eval:tools:diagnose` (the approval-gated live multi-step transition diagnostic, which establishes NO release gate; ONE live run has completed under its historical v2 classifier and its current v3 path has **not been run**). The hermetic upstream contract suite (`test:contract`) is implemented and runs inside `validate` via the `test` glob. `test:compatibility` and `test:adversarial` are hermetic (no network, credentials, or CollectivIQ calls) but are intentionally **excluded** from `validate`/CI and run on their own. The network-only `contract:*` commands, `eval:tools`, and `eval:tools:diagnose` must never be added to `validate` or CI; both `eval:*` commands default to a credential-free/network-free preflight; `eval:tools` has been run in five authorized campaigns, the latest being the completed **2026-09-01** campaign that scored the full corpus under its current **report v5 / checkpoint v4** form with all eight gates passing. Load suites are not implemented yet and must not be added to `validate`; see `.agent/instructions/validation.md`.

Do not claim a command exists or passed when it was unavailable. Do not install dependencies, regenerate a lockfile broadly, or choose package versions as a side effect of unrelated work.

## Architectural Invariants

- Keep the public API stateless. Do not reuse upstream threads until the specification's correlation, cleanup, and reliability prerequisites are met.
- Preserve full ordered conversation history and declared roles in the versioned JSON envelope for `promptMode: "protocol"` models (the default). The narrowly scoped `promptMode: "direct"` profile is the one intentional exception: it submits only the latest normalized user message and deliberately drops the protocol wrapper, system/developer instructions, assistant history, and earlier user turns (spec §8.4.1). Do not extend that lossy behavior to protocol models, and select prompt behavior from the validated `promptMode`, never a model-id string.
- Keep upstream schemas and endpoint knowledge inside the CollectivIQ adapter.
- Treat every upstream response as untrusted and validate it before use.
- Never execute tools in the gateway.
- Polling remains authoritative until request-scoped upstream event correlation is verified.
- Do not automatically retry `create_thread` or `process_message` without proven idempotency.
- Acquire concurrency capacity before creating an upstream thread, and release it on every exit path.
- Propagate client cancellation through pending upstream work and stop polling promptly.
- Return OpenAI-style public errors; never expose raw production upstream bodies.
- Do not present estimated token counts as exact usage.
- Do not broaden compatibility or enable opt-in beta tool mode by default without the corresponding tests, documentation, release evidence, and the separate default-enablement decision recorded in specification section 30.

## Critical Risk Index

Read the routed security and domain instructions before changing:

- credential handling, authentication, gateway binding, TLS assumptions, or metrics exposure;
- prompt serialization, role precedence, boundary generation, or content logging;
- tool parsing, JSON Schema validation, tool-choice enforcement, or fallback voting;
- upstream retries, timeouts, polling completion, duplicate-message selection, or cancellation;
- request/body/prompt/tool/upstream size limits, queues, concurrency, or rate limits;
- Redis idempotency, response caching, retention, or cross-replica state;
- SSE framing, keep-alives, terminal chunks, or disconnect handling;
- error/status mappings and any raw upstream diagnostic data.

## Approval Required

Ask for explicit user approval before initiating:

- destructive file operations, mass deletion, recursive moves, overwrite scripts, or cleanup commands;
- Git history changes, force pushes, shared-branch rebases, commits, pushes, tags, releases, or PR publication unless explicitly requested;
- deployment, package publishing, infrastructure mutation, secret-store changes, or other external writes;
- reading, printing, copying, rotating, or modifying real credentials or sensitive prompt/response data;
- major dependency upgrades, package-manager migrations, or broad lockfile regeneration outside the task;
- live CollectivIQ probes that transmit repository content or may create material cost/thread volume;
- enabling content logging, persistent prompt/response storage, or opt-in beta tool mode in a production configuration;
- large mechanical rewrites, generated-code refreshes, or formatting sweeps across many files.

State the exact action, why it is needed, what it can change or expose, and a safer alternative when one exists.

## Security and Privacy Musts

- Keep the upstream credentials — `COLLECTIVIQ_API_KEY` (bearer mode) or `COLLECTIVIQ_USERNAME`/`COLLECTIVIQ_PASSWORD` (password mode) — separate from gateway client keys, and redact all of them everywhere.
- Never commit real secrets or include them in commands, logs, fixtures, examples, errors, traces, snapshots, or session notes.
- Keep production content logging off. Development content logging requires both the explicit development-only switches and a prominent warning.
- Default binding is loopback. Non-loopback exposure requires explicit configuration plus authentication and deployment controls.
- Validate and bound request bodies, prompts, tool collections/schemas/arguments, upstream responses, queues, concurrency, and durations.
- Hash correlation identities only when needed; do not use prompt-derived permanent identifiers across trust boundaries.
- Sanitize every live upstream fixture before committing it.
- Treat prompt injection as an inherent trust-boundary risk, not something JSON delimiters eliminate.
- Preserve the default no-content-retention behavior; short-lived caches require an explicit design and security review.

Use `.agent/instructions/security.md` for the complete task checklist.

## Documentation Rule

Behavior, API contracts, architecture, upstream assumptions, configuration, security posture, deployment, testing, or terminology changes must update their owner documentation in the same task.

Agent instructions summarize how to work; they must not silently redefine the product specification. Use `.agent/instructions/documentation.md` for ownership and synchronization rules.

## Git and Change Hygiene

- Do not invent a branch model; none is defined by the specification.
- Do not commit, push, open or merge PRs, tag, publish, or deploy unless the user explicitly asks.
- Keep dependency versions locked once the package is initialized.
- Avoid unrelated refactors, formatting sweeps, generated files, and lockfile churn.
- Never discard user changes to make validation pass.

## Final Response Format

When finishing a change, report:

1. **Summary**: what changed and why.
2. **Files changed**: key files and their purpose.
3. **Validation**: exact commands run and results.
4. **Documentation**: owner docs updated, or why none were needed.
5. **Risks or follow-ups**: assumptions, unavailable checks, known limitations, or reviewer attention.

Keep the response concise, factual, and tied to the actual diff.

## Done Definition

A task is done when:

- The requested behavior is complete and scoped to the current delivery phase.
- Public and upstream contracts remain explicit and validated.
- Relevant tests or checks have run, or the reason they could not run is reported.
- Security, privacy, cancellation, limits, and approval gates have been considered where applicable.
- Owner documentation is accurate and internally consistent.
- The diff contains no unrelated rewrites, sensitive values, unsanitized fixtures, local-only artifacts, or weakened tests.
- A reviewer can understand what changed, why, how it was checked, and what remains uncertain.
